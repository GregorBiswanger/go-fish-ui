import { CardToImagePathPipe } from './card-to-image-path.pipe';

describe('CardToImagePathPipe', () => {
  it('create an instance', () => {
    const pipe = new CardToImagePathPipe();
    expect(pipe).toBeTruthy();
  });
});
